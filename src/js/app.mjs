function startApp() {
    // make magic in here!

    // Your entire app should not necessarily be coded inside this single function (though there's no penalty for that), 
    // so create and use/call additional functions from here
};

export default startApp;